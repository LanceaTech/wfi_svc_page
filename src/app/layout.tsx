import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Wealth Foundation Institute - Professional Business Services in Singapore",
  description: "Your trusted partner for complete business solutions in Singapore. From company registration to financial management, we provide comprehensive professional services.",
  keywords: ["Singapore company registration", "business services", "financial consulting", "corporate compliance", "accounting services"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}